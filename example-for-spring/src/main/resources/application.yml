spring:
  application:
    name: engagelab-sdk-server
  profiles:
    active: local

server:
  port: 9876

logging:
  level:
    root: info
    feign.Logger: debug

engagelab:
  api:
    # 请在 application-local.yml 中填写真实的 key（该文件已被 gitignore）
    app-key:
    master-secret:
    group-app-key:
    group-master-secret:
